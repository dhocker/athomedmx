#
# DMX test script
#
# Notes
#   All keywords, names are case insensitive.
#   The script starts with the initialization/setup phase
#   which includes all of the statements up to the
#   loop statement.
#

At 17:00

# Device channel definitions (channel name number 1-512)
channel tp64rgb  1
channel tp64mode 6
channel tp64dimmer 7

# Value definitions (value name integer 0-255...)
value rgb-off 0 0 0
value rgb-on 255 255 255
value off 0
value on 255
value rgb-mode 0

# Time definitions
define default-fade-time 3.0
define default-step-time 5.0
define periodtime 0.2
step-period periodtime

# Initial state (set channel value(s))
set tp64rgb rgb-off
set tp64mode rgb-mode
set tp64dimmer off
send

# Main loop
# The main loop runs until the app is terminated. The main statement ends
# the intialization/setup phase which is all the statements
# that precede the main statement.
main
    # Each step runs for step-time 5.0
    step default-fade-time default-step-time
        # Immediate changes
        set tp64rgb rgb-off
        set tp64dimmer on
        send
        # Values fade from their initial to target values over fadetime
        fade tp64rgb on 0 on
    step-end

    set tp64rgb rgb-off
    set tp64dimmer on
    send

    step default-fade-time default-step-time
        fade tp64rgb 0 on on
    step-end

    set tp64rgb rgb-off
    set tp64dimmer on
    send

    step default-fade-time default-step-time
        fade tp64rgb on on 0
    step-end

    set tp64dimmer off
    send
main-end

# If there is no loop statement, the script ends when there
# are no more statements.

# value bad 0 0 0 0 256